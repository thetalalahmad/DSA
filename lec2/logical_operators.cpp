#include <iostream>
using namespace std;
int main()
{
    /*
    int a = 5, b = 10, c = 15;
    // Logical AND
    cout << ((a > 0) && (b != 0) && (c <= 15)) << endl; // true
    // Logical OR
    cout << ((a > 5) || (b < 10) || (c >= 15)) << endl; // true
    */
    // Logical NOT
    //invert the logic
    // True = False, False = True
    //Non-zero =zero, zero = non-zero
    int a =10, b=0;
    cout << (!a) << endl;
    cout << (!b) << endl;
    return 0;
}